
import 'package:flutter/material.dart';

class IntrodPage extends StatelessWidget {
  const IntrodPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
